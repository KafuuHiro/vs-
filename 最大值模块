#include <iostream>
using namespace std;

int max(int a, int b)
{
    int n;
    n = (a + b + abs(a - b))/2;
    return n;
}

int main()
{
    int x,y,z;
    scanf_s("%d\n%d", &x, &y);
    z = max(x, y);
    printf("%d", z);
    return 0;
}
